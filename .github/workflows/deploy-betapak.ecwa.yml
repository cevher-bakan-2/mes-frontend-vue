name: 🚀 Build & Deploy Vue to FTP

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install deps
        run: npm ci

      - name: Build
        run: npm run build

      - name: FTP Deploy (dist -> /)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server:   ${{ secrets.FTP_HOST }}       # ← ZORUNLU
          username: ${{ secrets.FTP_USERNAME }}   # ← ZORUNLU
          password: ${{ secrets.FTP_PASSWORD }}   # ← ZORUNLU
          server-dir: /                           # önceki gibi kalabilir (çoğu hostta web kökü /public_html/ olur)
          local-dir: dist/                         # ./dist de olur; önerim sadece 'dist'
          # protocol: sftp                        # gerekiyorsa aç
          # port: 22                              # gerekiyorsa ekle
